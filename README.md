The ultimate goal of this is to implement multivariate density estimation via variance-reduced sketching (VRS) into the RECOVAR pipeline. To do so we will need to find a way to use it on noisey data.
